<legend>{{ text_headline }}</legend>
<form id="payment-existing-form" action="{{ form_submit }}" method="post" class="form-horizontal">
    <div class="buttons">
        <div class="pull-right">
            <input type="submit" value="{{ button_confirm }}" id="button-confirm" data-loading-text="{{ text_loading }}"
                   class="btn btn-primary"/>
        </div>
    </div>
</form>

<div id="payment-errors">
</div>

